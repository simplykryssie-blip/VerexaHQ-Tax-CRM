From 812706e61ef5b0b69f677e48a6344cad25150758 Mon Sep 17 00:00:00 2001
From: OpenAI Codex <codex@openai.com>
Date: Sat, 1 Aug 2026 09:00:47 +0900
Subject: [PATCH] Fix CI lint errors

---
 components/app-shell/sidebar.tsx              | 30 +++++++++++--------
 .../clients/tabs/ClientPersonalInfoTab.tsx    |  2 +-
 features/intake/organizer-form.tsx            |  2 +-
 .../templates/organizer-structure-viewer.tsx  |  1 -
 features/workflows/builder-panel.tsx          |  2 +-
 lib/data/search.ts                            |  6 ++--
 6 files changed, 24 insertions(+), 19 deletions(-)

diff --git a/components/app-shell/sidebar.tsx b/components/app-shell/sidebar.tsx
index cc104d2..48b4a95 100644
--- a/components/app-shell/sidebar.tsx
+++ b/components/app-shell/sidebar.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { useEffect, useState } from "react";
+import { useSyncExternalStore } from "react";
 import Link from "next/link";
 import { ChevronsLeft, ChevronsRight } from "lucide-react";
 import { LogoMark } from "@/components/logo";
@@ -8,21 +8,27 @@ import { NavList } from "@/components/app-shell/nav-list";
 import { cn } from "@/lib/utils";
 
 const COLLAPSE_KEY = "verexa-tax-office-sidebar-collapsed";
+const COLLAPSE_EVENT = "verexa-tax-office-sidebar-change";
 
-export function Sidebar() {
-  const [collapsed, setCollapsed] = useState(false);
+function subscribeToCollapsePreference(onStoreChange: () => void) {
+  window.addEventListener("storage", onStoreChange);
+  window.addEventListener(COLLAPSE_EVENT, onStoreChange);
+  return () => {
+    window.removeEventListener("storage", onStoreChange);
+    window.removeEventListener(COLLAPSE_EVENT, onStoreChange);
+  };
+}
 
-  useEffect(() => {
-    const stored = window.localStorage.getItem(COLLAPSE_KEY);
-    if (stored === "1") setCollapsed(true);
-  }, []);
+function getCollapsePreference() {
+  return window.localStorage.getItem(COLLAPSE_KEY) === "1";
+}
+
+export function Sidebar() {
+  const collapsed = useSyncExternalStore(subscribeToCollapsePreference, getCollapsePreference, () => false);
 
   function toggle() {
-    setCollapsed((prev) => {
-      const next = !prev;
-      window.localStorage.setItem(COLLAPSE_KEY, next ? "1" : "0");
-      return next;
-    });
+    window.localStorage.setItem(COLLAPSE_KEY, collapsed ? "0" : "1");
+    window.dispatchEvent(new Event(COLLAPSE_EVENT));
   }
 
   return (
diff --git a/components/clients/tabs/ClientPersonalInfoTab.tsx b/components/clients/tabs/ClientPersonalInfoTab.tsx
index f6bf8c0..56d6680 100644
--- a/components/clients/tabs/ClientPersonalInfoTab.tsx
+++ b/components/clients/tabs/ClientPersonalInfoTab.tsx
@@ -32,7 +32,7 @@ export function ClientPersonalInfoTab({ client, workspaceId, onUpdate }: ClientP
       lastName: client.last_name,
       suffix: client.suffix || "",
       dateOfBirth: client.date_of_birth || "",
-      filingStatus: (client.filing_status as any) || "",
+      filingStatus: (client.filing_status as PersonalInfoInput["filingStatus"]) || undefined,
       email: client.email || "",
       occupation: client.occupation || "",
       drivingLicenseNumber: client.id_number || "",
diff --git a/features/intake/organizer-form.tsx b/features/intake/organizer-form.tsx
index 93fa398..495dbed 100644
--- a/features/intake/organizer-form.tsx
+++ b/features/intake/organizer-form.tsx
@@ -271,7 +271,7 @@ export function OrganizerForm({
   // section are evaluated per-instance below instead.
   const globalValues = answers;
 
-  let visibleRequiredKeys: string[] = [];
+  const visibleRequiredKeys: string[] = [];
   const answeredKeys = new Set(Object.keys(answers).filter((k) => answers[k] !== null && answers[k] !== undefined && answers[k] !== ""));
 
   return (
diff --git a/features/templates/organizer-structure-viewer.tsx b/features/templates/organizer-structure-viewer.tsx
index e8cd7c4..2cc5607 100644
--- a/features/templates/organizer-structure-viewer.tsx
+++ b/features/templates/organizer-structure-viewer.tsx
@@ -38,7 +38,6 @@ export function OrganizerStructureViewer({
 
   useEffect(() => {
     let cancelled = false;
-    setLoading(true);
     const supabase = createClient();
     Promise.all([
       supabase.from("form_sections").select("*").eq("template_version_id", templateVersionId).order("sort_order"),
diff --git a/features/workflows/builder-panel.tsx b/features/workflows/builder-panel.tsx
index 5542bcf..10865fe 100644
--- a/features/workflows/builder-panel.tsx
+++ b/features/workflows/builder-panel.tsx
@@ -154,7 +154,7 @@ export function WorkflowBuilderPanel({ definition, nodes, connections }: { defin
       <Card>
         <CardHeader>
           <CardTitle className="text-base">Nodes</CardTitle>
-          <CardDescription>Each node is a step. Give every node a unique key (e.g. "send_welcome_email").</CardDescription>
+          <CardDescription>Each node is a step. Give every node a unique key (e.g. &quot;send_welcome_email&quot;).</CardDescription>
         </CardHeader>
         <CardContent className="space-y-3">
           {nodes.map((n) => (
diff --git a/lib/data/search.ts b/lib/data/search.ts
index 98b52fd..85c2e93 100644
--- a/lib/data/search.ts
+++ b/lib/data/search.ts
@@ -72,8 +72,8 @@ export async function globalSearch(
 
   return {
     clients: (clientsRes.data as Client[]) ?? [],
-    engagements: (engagementsRes.data as any[]) ?? [],
-    intakes: (intakesRes.data as any[]) ?? [],
-    documents: (documentsRes.data as any[]) ?? [],
+    engagements: (engagementsRes.data as GlobalSearchResult["engagements"]) ?? [],
+    intakes: (intakesRes.data as GlobalSearchResult["intakes"]) ?? [],
+    documents: (documentsRes.data as GlobalSearchResult["documents"]) ?? [],
   };
 }
-- 
2.51.1

